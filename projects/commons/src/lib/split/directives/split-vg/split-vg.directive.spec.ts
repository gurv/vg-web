import { SplitVgDirective } from './split-vg.directive';

describe('SplitVgDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitVgDirective();

    expect(directive).toBeTruthy();
  });
});
